# 1.1.1 版本归档

## 当时的问题与需求

用户确认新的 Saber 人格提示词，需要正式替换并保持原任务操作协议。

## 对应改动

启用确认的人格提示词，完整聊天与浮动助手共用；历史消息不重写。

## 证据与验证边界

此记录于 2026-09-21 回溯整理，不是当时的 Git 提交。摘要依据项目对话与独立更新说明。归档时已核对源码 Manifest 与 APK 的版本号，并检查 ZIP 完整性；未重新执行旧版测试或安装手机。下面原文的测试数字是当时记录，不等于本次重新验证。后续版本可能再次修改此版行为。

## 原始说明（原文保留）

# 尼尔机械济援 1.1.1

已启用用户确认的 Saber 语言提示词。下次对话生效，完整聊天与浮动助手共用。保留任务操作与九种表情 JSON 协议。天气本地规则台词沿用 1.1。

原应用 ID、签名不变，覆盖安装即可，勿先卸载；不修改已有聊天、习惯记忆或待办。新提示词会参与后续回答，不会重写旧消息。

构建、签名和 167 项 JVM 检查通过；尚未调用付费 DeepSeek 接口或完成手机安装验证。

